  git config --global user.email "piaoyaosuifengji@sina.com"
  git config --global user.name "piaoyaosuifengji"


touch Makefile.am
运行autoscan  并：mv configure.scan configure.ac


设置Makefile.am

autoreconf -i
./configure

最后修改makefile

all: config.h
	$(MAKE) $(AM_MAKEFLAGS) all-am
	./$(bin_PROGRAMS)
	
	
d: 
	gdb $(bin_PROGRAMS)
